<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Neruppu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/purple_500</item> -->
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.Neruppu.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#222222</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Neruppu</item>
    </style>
</resources>
